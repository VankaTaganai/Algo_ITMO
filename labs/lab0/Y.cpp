#include <bits/stdc++.h>

using namespace std;

int main() {
	freopen("yield.in", "r", stdin);
	freopen("yield.out", "w", stdout);

	double a, b;
	cin >> a >> b;
	cout << fixed << setprecision(10) << a + b;
}
